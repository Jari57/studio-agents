{
  "name": "studio-agents-ai",
  "version": "2.0.2",
  "description": "Studio Agents - AI Music Studio Platform",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "echo Starting frontend... && cd frontend && npm run dev",
    "backend": "echo Starting backend... && cd backend && npm start",
    "kill": "powershell -Command \"Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force\" && echo All Node processes killed",
    "fresh": "npm run kill && timeout /t 2 /nobreak && echo Starting fresh... && npm run backend",
    "build": "cd frontend && npm ci && npm run build",
    "build:clean": "powershell -File scripts/cleanup-build.ps1 && npm run build",
    "clean": "powershell -File scripts/cleanup-build.ps1",
    "install:all": "npm ci && cd backend && npm ci && cd ../frontend && npm ci",
    "start": "cd backend && npm start"
  },
  "postinstall": "cd backend && npm install && if exist ..\\frontend\\dist (rd /s /q backend\\public & mkdir backend\\public & xcopy /E /I /Y ..\\frontend\\dist backend\\public)"
}
